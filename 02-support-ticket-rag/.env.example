LLM_PROVIDER=mock

# Public portfolio URL when the API is deployed (no trailing slash)
# FRONTEND_ORIGIN=https://your-portfolio.vercel.app

# Only needed when LLM_PROVIDER=gemini
# GOOGLE_API_KEY=your-key

# Only needed when LLM_PROVIDER=openai
# OPENAI_API_KEY=your-key
